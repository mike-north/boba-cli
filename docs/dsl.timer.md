<!-- Do not edit this file. It is automatically generated by API Documenter. -->

[Home](./index.md) &gt; [@boba-cli/dsl](./dsl.md) &gt; [timer](./dsl.timer.md)

## timer() function

Create a timer component builder.

**Signature:**

```typescript
declare function timer(options: TimerBuilderOptions): ComponentBuilder<TimerModel>;
```

## Parameters

<table><thead><tr><th>

Parameter


</th><th>

Type


</th><th>

Description


</th></tr></thead>
<tbody><tr><td>

options


</td><td>

[TimerBuilderOptions](./dsl.timerbuilderoptions.md)


</td><td>

Configuration options for the timer


</td></tr>
</tbody></table>

**Returns:**

[ComponentBuilder](./dsl.componentbuilder.md)<!-- -->&lt;[TimerModel](./timer.timermodel.md)<!-- -->&gt;

A `ComponentBuilder` ready to use with `AppBuilder.component`

## Remarks

Creates a `ComponentBuilder` wrapping the `@boba-cli/timer` package. The timer counts down from the specified timeout and can be started, stopped, or toggled programmatically.

## Example 1

Basic usage with auto-start:

```typescript
const app = createApp()
  .component('countdown', timer({ timeout: 60000 })) // 1 minute
  .view(({ components }) => hstack(
    text('Time remaining: '),
    components.countdown
  ))
  .build()
```

## Example 2

With custom interval and manual start:

```typescript
const app = createApp()
  .component('countdown', timer({
    timeout: 30000,      // 30 seconds
    interval: 100,       // Update every 100ms
    autoStart: false     // Don't start immediately
  }))
  .view(({ components }) => components.countdown)
  .build()
```

